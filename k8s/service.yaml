apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  labels:
    app: wisecow
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 80          # cluster port (Ingress will connect to 80)
      targetPort: 4499  # container port
  type: ClusterIP

